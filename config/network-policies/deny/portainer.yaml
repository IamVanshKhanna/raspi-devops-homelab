apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: portainer-deny-all
  namespace: portainer
  labels:
    app.kubernetes.io/name: portainer
    policy-type: deny-by-default
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress