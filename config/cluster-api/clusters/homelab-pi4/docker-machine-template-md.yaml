apiVersion: infrastructure.cluster.x-k8s.io/v1
kind: DockerMachineTemplate
metadata:
  name: homelab-pi4-md-0
  namespace: homelab-clusters
spec:
  template:
    spec:
      extraMounts:
        - containerPath: /var/run/docker.sock
          hostPath: /var/run/docker.sock