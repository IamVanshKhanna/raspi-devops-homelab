# Traefik Static Configuration
# https://doc.traefik.io/traefik/getting-started/configuration-overview/

global:
  checkNewVersion: false
  sendAnonymousUsage: false

api:
  dashboard: true
  insecure: false  # Dashboard served via router with auth

log:
  level: INFO
  filePath: /logs/traefik.log

accessLog:
  filePath: /logs/access.log
  bufferingSize: 100

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    address: ":443"
  metrics:
    address: ":8082"

certificatesResolvers:
  letsencrypt:
    acme:
      # Set ACME_EMAIL in your .env file
      email: "${ACME_EMAIL}"
      storage: /certs/acme.json
      # DNS-01 challenge with Cloudflare (v1.4+)
      # Uncomment and configure for DNS-01 (closes port 80):
      # dnsChallenge:
      #   provider: cloudflare
      #   delayBeforeCheck: 30
      #   resolvers:
      #     - "1.1.1.1:53"
      #     - "8.8.8.8:53"
      # Fallback to HTTP-01 (requires port 80 open):
      httpChallenge:
        entryPoint: web

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: proxy
  file:
    filename: /dynamic.yml
    watch: true

metrics:
  prometheus:
    entryPoint: metrics
    addEntryPointsLabels: true
    addServicesLabels: true