# Default Deny Ingress - postgres-system
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: postgres-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
