apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: Metal3MachineTemplate
metadata:
  name: homelab-pi5-cp
  namespace: homelab-clusters
spec:
  template:
    spec:
      # Metal3 machine provider spec for control plane
      # Will be populated with BareMetalHost references