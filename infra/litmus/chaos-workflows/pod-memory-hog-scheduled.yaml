apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: litmus-pod-memory-hog-scheduled
  namespace: litmus
spec:
  templates:
    - name: pod-memory-hog-chaos
      container:
        image: litmuschaos/litmus-checker:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            kubectl apply -f /experiments/pod-memory-hog.yaml
            sleep 5
            kubectl wait --for=condition=Completed chaosengine/pod-memory-hog -n litmus --timeout=300s
    - name: cleanup
      container:
        image: bitnami/kubectl:latest
        command: ["/bin/sh", "-c"]
        args:
          - kubectl delete chaosengine pod-memory-hog -n litmus --ignore-not-found=true
  entrypoint: pod-memory-hog-chaos
---
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: pod-memory-hog-weekly
  namespace: litmus
spec:
  schedule: "0 3 * * 3"  # Weekly on Wednesday at 3 AM
  concurrencyPolicy: "Forbid"
  startingDeadlineSeconds: 300
  workflowSpec:
    workflowTemplateRef:
      name: litmus-pod-memory-hog-scheduled