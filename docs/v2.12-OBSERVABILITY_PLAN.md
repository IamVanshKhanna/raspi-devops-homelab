# v2.12 Observability Consolidation Plan

## Current State Analysis

### Dashboards (7 → Target: 4)
| Dashboard | Size | Focus | Action |
|-----------|------|-------|--------|
| `system-overview.json` | 10KB | Cluster/node/container health | ✅ Keep as "System Overview" |
| `containers.json` | 7KB | Container-level metrics | **Merge** into System Overview |
| `pi-power-monitoring.json` | 11KB | Pi hardware power | ✅ Keep (specialized) |
| `capacity-planning.json` | 12KB | Resource trends, forecasting | ✅ Keep (specialized) |
| `slo-error-budget.json` | 8KB | Error budget tracking | **Merge** into "SLO Dashboard" |
| `tracing-overview.json` | 11KB | Distributed tracing | ✅ Keep (specialized) |
| `correlation-id-debugging.json` | 10KB | Correlation ID tracing | **Merge** into Tracing Overview |

### Alert Rules (Duplication)
- `burn-rate-alerts.yaml` + `slo-definitions.yaml` = **Duplicate SLO burn alerts**
- `homelab.yml` + `k8s-cluster.yaml` + `pi-power-alerts.yaml` = Overlapping infrastructure alerts
- Need unified alert routing in Alertmanager

### Log Retention (Loki)
- Current: `config/loki/retention-policy.yaml` - needs validation

### Trace Sampling (Tempo)
- Current: `config/otel/` - needs sampling policy review

---

## Consolidation Plan

### 1. Dashboards: 7 → 4
| New Dashboard | Sources | Panels |
|---------------|---------|--------|
| **System Overview** | system-overview + containers | Cluster health, nodes, containers, workloads |
| **SLO & Error Budgets** | slo-error-budget + (new burn rate viz) | SLO status, error budget, burn rate trends |
| **Tracing & Correlation** | tracing-overview + correlation-id-debugging | Service graph, latency, correlation IDs |
| **Pi Power & Capacity** | pi-power-monitoring + capacity-planning | Power, thermal, capacity trends |

### 2. Alert Rules: Deduplicate
- Remove duplicate SLO burn alerts from `slo-definitions.yaml` (keep `burn-rate-alerts.yaml`)
- Merge `homelab.yml` + `k8s-cluster.yaml` → `infrastructure-alerts.yaml`
- Keep `pi-power-alerts.yaml` separate (specialized)
- Add missing runbook references

### 3. Log Retention
- Verify `config/loki/retention-policy.yaml` is applied
- Add Loki retention metrics to System Overview dashboard

### 4. Trace Sampling
- Document current sampling rate in `config/otel/`
- Add sampling overhead panel to Tracing dashboard

---

## Implementation Tasks

| Task | File | Status |
|------|------|--------|
| Create consolidated System Overview dashboard | `config/grafana/provisioning/dashboards/system-overview.json` | ⬜ |
| Create SLO & Error Budgets dashboard | `config/grafana/provisioning/dashboards/slo-overview.json` | ⬜ |
| Create Tracing & Correlation dashboard | `config/grafana/provisioning/dashboards/tracing-correlation.json` | ⬜ |
| Create Pi Power & Capacity dashboard | `config/grafana/provisioning/dashboards/pi-power-capacity.json` | ⬜ |
| Remove old dashboards | containers.json, slo-error-budget.json, correlation-id-debugging.json | ⬜ |
| Update dashboard.yml provisioning | `config/grafana/provisioning/dashboards/dashboard.yml` | ⬜ |
| Deduplicate Prometheus rules | Merge/remove duplicate SLO alerts | ⬜ |
| Consolidate infrastructure alerts | Merge homelab.yml + k8s-cluster.yaml | ⬜ |
| Verify Loki retention | Check config/loki/retention-policy.yaml | ⬜ |
| Document trace sampling | Update config/otel/README.md | ⬜ |
| Update runbooks README | Add new dashboard references | ⬜ |