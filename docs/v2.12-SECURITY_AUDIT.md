# v2.12 Security Hardening Audit

> **Generated:** 2026-06-10
> **Branch:** feature/v2.12-security
> **Target:** Zero CRITICAL/HIGH in production, cert rotation automated, pen test validated

---

## Current Security Posture

| Component | Status | Gap |
|-----------|--------|-----|
| **Kyverno Policies** | ✅ 18 policies | No image signature verification (cosign) |
| **CrowdSec** | ✅ Deployed | No custom bouncers, limited parser coverage |
| **NetworkPolicies** | ✅ Zero-trust | Missing egress policies for all namespaces |
| **Secrets Rotation** | ✅ Weekly workflow | Dry-run only by default, no ExternalSecrets sync validation |
| **Cert Management** | ✅ cert-manager + Ansible | No automated expiry alerting < 30 days |
| **Supply Chain** | ✅ SBOM + Cosign + Trivy | Trivy only on compose, not Helm/ArgoCD images |
| **mTLS** | ⚠️ Linkerd installed | Not enforced globally (permissive mode) |
| **Pen Testing** | ❌ None scheduled | No automated chaos/security validation |

---

## Priority Actions

### 1. Image Signature Verification (Kyverno + Cosign) 🔴 **High**
**Gap:** No runtime verification that images match signed digests

**Solution:** Add Kyverno policy to verify cosign signatures
```yaml
# config/kyverno/policies/13-require-cosign-verification.yaml
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-cosign-verification
spec:
  validationFailureAction: Enforce
  rules:
    - name: verify-image-signature
      match:
        any:
          - resources:
              kinds: ["Pod"]
      verifyImages:
        - image: "*"
          key: |-
            -----BEGIN PUBLIC KEY-----
            ...
            -----END PUBLIC KEY-----
          annotations:
            - "cosign.sigstore.dev/signature"
```

### 2. mTLS Enforcement (Linkerd) 🟡 **Medium**
**Current:** Linkerd installed in permissive mode
**Target:** Strict mTLS for all meshed namespaces

```yaml
# config/linkerd/mTLS-strict.yaml
apiVersion: linkerd.io/v1alpha2
kind: ServerAuthorization
metadata:
  name: default-mtls
  namespace: linkerd
spec:
  server:
    selector:
      matchLabels:
        linkerd.io/control-plane-ns: linkerd
  client:
    meshTLS:
      authentications:
        - identity:
            suffix: "*.service-identity.linkerd.cluster.local"
```

### 3. Egress NetworkPolicies 🟡 **Medium**
**Gap:** Only ingress policies exist, no egress controls

**Target:** Default deny egress + explicit allow per namespace

### 4. Cert Expiry Monitoring 🟡 **Medium**
**Current:** Manual Ansible playbook
**Target:** Prometheus alerts + auto-renewal validation

### 5. Trivy Coverage Expansion 🟡 **Medium**
**Current:** Only Docker Compose images
**Target:** All Helmfile, ArgoCD, and base images

---

## Implementation Plan

### Week 1: Runtime Security
- [ ] Add cosign verification Kyverno policy
- [ ] Enable Linkerd strict mTLS
- [ ] Add egress NetworkPolicies per namespace

### Week 2: Monitoring & Automation
- [ ] Add cert-expiry Prometheus alerts
- [ ] Expand Trivy workflow to all image sources
- [ ] Automate secrets rotation (remove dry-run default)

### Week 3: Validation
- [ ] Run full Trivy scan (all images)
- [ ] Pen test via LitmusChaos security experiments
- [ ] Verify cert rotation end-to-end

---

## Commands Reference

```bash
# Check Kyverno policy status
kubectl get clusterpolicy,policy -A

# Verify Linkerd mTLS
linkerd check --proxy
linkerd viz stat deployment -n <namespace>

# Check NetworkPolicies
kubectl get networkpolicy -A -o wide

# Cert expiry check
kubectl get certificates -A -o jsonpath='{range .items[*]}{.metadata.namespace}{\"\\t\"}{.metadata.name}{\"\\t\"}{.status.notAfter}{\"\\n\"}{end}'

# Trivy scan all images
trivy image --severity HIGH,CRITICAL <image>

# Secrets rotation dry-run
INFISICAL_PROJECT_ID=xxx INFISICAL_TOKEN=xxx DRY_RUN=true ./scripts/secrets-rotation.sh
```

---

## Compliance Targets (v2.12)

| Metric | Target | Current |
|--------|--------|---------|
| Trivy CRITICAL in prod | 0 | Unknown |
| Trivy HIGH in prod | 0 | Unknown |
| mTLS coverage | 100% meshed | ~60% |
| Egress policies | All namespaces | 0 |
| Cert expiry alert | <30 days | Manual |
| Secrets rotation | Automated 90d | Dry-run only |
| Pen test frequency | Quarterly | Never |