apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nextcloud-db
  namespace: argocd
spec:
  project: homelab
  source:
    repoURL: https://charts.zalando.io
    targetRevision: "1.13.0"
    chart: zalando/postgres-operator
    helm:
      valueFiles:
      - ../../helmfile/values/defaults.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: databases
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m